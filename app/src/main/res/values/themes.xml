<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.SmartFarm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="colorPrimary">@color/teal</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item> <!-- Ikon status bar akan terang -->
        <item name="android:windowBackground">@android:color/white</item> <!-- Background putih -->
    </style>

    <style name="Theme.SmartFarm" parent="Base.Theme.SmartFarm" />
</resources>
